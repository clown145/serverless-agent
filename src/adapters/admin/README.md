# Admin Adapter

Admin messages are local operator messages used by scripts and curl-based development flows.

The browser console should prefer the WebUI adapter, but both adapters normalize into the same `InternalMessage` shape.
