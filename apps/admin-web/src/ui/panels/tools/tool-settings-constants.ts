// Keep these in sync with backend constants in
// src/storage/repositories/tool-settings-types.ts
export const MIN_TOOL_CALLS_PER_RUN = 1;
export const MAX_TOOL_CALLS_PER_RUN = 100;

export const MIN_MODEL_STEPS_PER_RUN = 1;
export const MAX_MODEL_STEPS_PER_RUN = 500;
